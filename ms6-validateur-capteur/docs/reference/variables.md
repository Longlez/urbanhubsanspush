# Variables d'environnement

- `RABBITMQ_HOST` : hôte RabbitMQ utilisé par `ms6-validateur-capteur`.
- `RABBITMQ_PORT` : port RabbitMQ, par défaut `5672`.
- `RABBITMQ_OUTPUT_QUEUE` : file de sortie pour les messages validés, par défaut `validated_queue`.
